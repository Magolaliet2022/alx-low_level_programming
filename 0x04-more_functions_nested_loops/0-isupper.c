#include "main.h"

/**
 * _isuper - evaluate if letter is uppercase .
 * @c: print int
 * Return always 0 .
*/

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

else
{
return (0);
}

}
